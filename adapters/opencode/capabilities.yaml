schema_version: 1
adapter: opencode
capabilities:
  prompts: supported
  shell_access: supported
  hooks: supported
  dynamic_suggestions: supported
  structured_output: supported
fallbacks:
  prompts: "Render static guidance and explicit runectx command proposals."
  shell_access: "Show command steps and candidate data without execution."
  hooks: "Run explicit runectx validate at review checkpoints."
  dynamic_suggestions: "Use runectx completion suggest output as numbered choices."
  structured_output: "Emit plain text summary with equivalent CLI flags/values."
