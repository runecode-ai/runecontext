# Codex Flow: change new

Map conversational intake to:

```sh
runectx change new --title "<title>" --type <type> [--size <size>] [--shape <minimum|full>] [--bundle <bundle-id>] [--description "<text>"] [--path <project-root>]
```
