schema_version: 1
adapter: generic
capabilities:
  prompts: optional
  shell_access: optional
  hooks: optional
  dynamic_suggestions: optional
  structured_output: optional
fallbacks:
  prompts: "Use static guidance plus explicit runectx command proposals."
  shell_access: "Provide user-run CLI steps and candidate values."
  hooks: "Document explicit runectx validate checkpoints."
  dynamic_suggestions: "Use runectx completion suggest providers manually."
  structured_output: "Use plain text summaries and equivalent CLI arguments."
